# 材料服务整改交接（2026-08-21）

## 本轮目标

统一材料分服务的职责说明，并提升 1111「材料—配比组分设计」和 1115「材料发现」在计算前的模型说明页：说明本轮将调用的真实工具、描述符、模型架构与公式；结果区仅陈述实际执行且用于结论的结果。

## 已完成

### 服务职责（已按老板方案调整）

- 1100：前端需求定义；将应用场景与工况转成定量材料指标和验证标准。
- 1105：成熟材料选型与核验；在材料库/标准牌号范围内检索、比对并形成选型证据链。
- 1111：特定已知材料体系的组分设计；后续可扩展至不止合金，但名称暂不改。
- 1115：材料候选尚未确定时的探索性材料发现；后续可扩展至不止无机晶体。
- 1112、1113 的名称暂不改。

职责文案原则：先用一句话说明服务做什么，再写可做事项；不写“不做什么”；避免把不同服务都描述为泛泛的“筛选”。

### 1115：材料发现

代码：`material_discovery/src/material_workflow/presentation.py`

- 在计算启动前增加「计划计算链与模型定义」。
- 依本轮配置介绍 MatterGen → pymatgen → ALIGNN（仅请求了属性时）→ MatterSim（仅启用时）。
- 用中文解释输入、输出与用途；属性名已从内部 `jv_*`/`band_gap` 等标识改为带隙、体积模量、剪切模量、介电常数、电子/空穴有效质量等用户表述。
- 已补充 MatterGen 条件扩散、周期最小像距离/空间群、ALIGNN 图消息传递、MatterSim 势能/形成能/高于凸包能公式。
- GIF 已改为复用 GLB 的元素配色、2×2×2 超胞和多面体视觉语言；GLB 保持原有满意的渲染策略不动。
- 后处理内容仅展示真实产生且进入结论的模型/数据；不再写“本轮未执行”。

### 1111：配比组分设计

代码：`material_composition_optimization/src/alloy_workflow/presentation.py`

- 在计算启动前增加成分归一化采样、真实描述符、离线模型对比与 MLP 架构说明。
- 描述符基于实际实现：元素原子分数、元素数、理想混合熵、平均原子序数、平均价电子浓度（VEC）、制备工艺编码、评价温度和温度缺失标记。
- 明确 MLP 为 64 → 32、ReLU、早停；其用于离线比较和解释非线性成分—性能映射。
- 明确本轮当前数值来自离线验证最优的 ExtraTrees 五随机种子集成，不误称 MLP 的计算结果。
- 增加 Dirichlet 采样、描述符、MLP 前向传播、模型选择、集成均值/离散度、数据适用域距离与综合排序公式。
- 采样输入输出、相类别等已改成中文业务含义：如固溶体主导（SS）、金属间相（IM）、混相（SS+IM）。

### 治理规则

代码：`/data/se42/docs/material-service-governance/SKILL.md`

已补充：计算前应介绍本轮计划调用的真实模型链；计算后只陈述实际执行并用于结论的模型、数据和结果。不要对未执行模型特意显示“本轮未执行”。

## 2026-08-21 续办完成：重复区块与公式块传输

- 1115 的结果摘要此前再次调用了“问题描述 → 输入变量 → 计算链 → 指标说明”的报告生成器，导致计划说明之后重复显示 1–4。现已移除该重复调用；结果区仅保留实际候选表、结论与已产出的性质初筛。
- 前端确认完整报告不能作为单个 WebSocket 文本帧直发：瞬时大量内容会造成 Markdown 渲染错乱。1115 的 `team_config.py::_stream_authoritative_markdown()` 已恢复为既有 LLM token 流式转发；1111 的 `main.py` 计划说明也已改为走同一通道。所有文字、表格、符号和 `$$...$$` 公式均通过该转发器发送。
- `docs/material-service-governance/SKILL.md` 已记录该前端约束：后续不得将完整 Markdown 报告整块直发，转发提示必须要求逐字保留 Markdown、表格、公式和链接。
- 静态检查确认 1111 有 12 个独立 `$$...$$` 块，1115 有 8 个；部署后应以页面复测确认最终块公式的居中渲染。
- 16:23 曾因恢复流式转发时误将 `llm` 传入 `stream_discovery_progress()` 而阻止 1115 启动；该函数只接收 `websocket、task_dir、request_id`，现已修复。资产说明的流式调用也已补回必需的 `llm` 参数。

## 验证与 Git 状态

- 本次涉及的 1115 文件及 1111 展示文件均已通过 `python -m py_compile`；`git diff --check` 通过。
- 各服务的契约测试在当前基础环境的收集阶段被缺失依赖 `google.generativeai` 阻断，尚未执行断言；这不是本次改动产生的测试失败。
- `git diff --check` 通过。
- 当前分支：`update/material-services-2026-08`。
- 用户明确要求：**未明确要求时不要 push Git**。
- 已推送提交：`138e53b`、`ce71cdf`。
- 本地未推送提交：`7770ac8`（此前 push 被拒绝）。
- 当前仍有未提交改动，至少包括本轮两个 `presentation.py`；另有 `material_composition_optimization/src/alloy_workflow/identity.py` 与 `material_database/src/service_identity.py` 改动，非本轮应谨慎保留，不要覆盖。

## 新对话建议的第一句话

“请先阅读 `AGENTS.md` 和本仓库 `HANDOFF_2026-08-21.md`，继续复测 1115/1111 的 LLM token 流式 Markdown 渲染；不要 push Git，且不要覆盖已有未提交改动。”

## 2026-08-24 续办：1115 热力学参考与凸包缓存

- 原先 `mp_api` 路径将 MatterSim 候选能量与 Materials Project DFT 竞争相能量直接混合，造成形成能与 `E_hull` 能量基准不一致。1115 已默认改用 MatterGen 的 `ReferenceMP2020Correction`（MP + Alexandria，MP2020 校正）统一参考体系；`mp_api` 仅保留作诊断回退，不得作为客户稳定性数值。
- 官方参考压缩库展开后约 3.85 GB，现持久缓存于 `/data/mattersim_reference_cache/reference_MP2020correction`，不再每个任务重复解压。
- 新增按允许元素体系持久化的凸包缓存 `/data/mattersim_phase_diagram_cache`。当前已预热：Co-Cr-Fe-Mn-Ni（1175 条参考相）、Nb-Mo-Ta-W（693）、Li-P-S（148）、Li-P-S-Cl（222）、Li-La-Zr-O（234）；五个凸包对象合计约 4.7 MB。候选若仅使用允许体系的元素子集，仍复用该父体系凸包。
- 已直接复测 `MnFeCoNi` 候选：形成能 `-0.0121 eV/atom`，`E_hull=0.0624 eV/atom`，高于 `0.05 eV/atom` 阈值，故稳定性初筛未通过。此前页面的 `6.2252 eV/atom` 是混合能量基准的失真值。
- 1115 性质初筛卡保留 ALIGNN 所有预测，但对近零/负带隙、有效质量和介电常数的近金属失效情形，以及弹性模量不自洽导致的硬度截断，给出明确解读，不再将回归外推作为可采信物理结论。
- 验证：`python -m py_compile`、`PYTHONPATH=. pytest -q tests/test_alignn_property_screening.py`（8 passed）、`git diff --check` 通过。`tests/test_service_contract.py` 在收集阶段因基础环境缺少 `google.generativeai` 失败，未进入断言。

## 2026-09-01 续办：1111 热端镍基合金用户流程

- 1111 新增 `ni_superalloy_hot_end` 路径，和 HEA/MPEA 保持独立：输入为 wt.%、制造路线、热处理、温度和蠕变载荷；输出为短时 UTS、0.2% proof strength、蠕变断裂寿命及延伸率辅助信息。当前数据未包含可训练硬度标签，页面明确显示“当前目录未收录”。
- 用户只提出“镍基/蠕变/单晶叶片”等意图时，WebSocket 和上游角色均先返回结构化条件清单；不会擅自填入热处理、温度、载荷或成分边界。输入完整后，候选只从同路线剑桥来源合金锚点的局部成分邻域产生，并由 Ni 平衡。
- 结果页保留既有 `start → progress → 内容区 → result → end` 协议与 `FILAMENT_SELECTION_OPTIMIZATION`。新增两个任务资产：候选筛选漏斗、短时强度—蠕变寿命对比；均通过现有 MinIO 或本地任务资产回退路径发布，并在 Markdown token 流中嵌入。
- 端到端模型冒烟：单晶、950 °C、250 MPa、20 个候选，成功输出 12 个短名单及两张 PNG。模型接口/热端生成单测 4 项通过；1111 契约测试 16 项通过：`PYTHONPATH=. ALLOY_PRESENTATION_LLM=false /home/ubuntu/miniconda3/envs/ai4m-service-py310/bin/python -m unittest -q tests/test_service_contract.py`。`git diff --check` 通过。
- 尚未重启任何 tmux 或线上服务；部署时由用户在既有 1111 tmux 中重启服务。

## 2026-09-01 续办：热端默认工况与公开图表路径

- 已明确识别“航空高温合金”“航空航天高温合金”“发动机热端”等请求并路由至 `ni_superalloy_hot_end`。当用户未给出详细条件时，服务不再停在补充清单：采用可见且可覆盖的平台默认首轮工况——单晶、950 °C、250 MPa、已记录的三阶段热处理，以及 Ni–Cr–Co–Al–Ta–W–Ti–Mo–C–B 保守 wt.% 边界。
- 这些字段在输入计划页标为“平台默认热端工况”；用户提供任一字段时，该字段由用户值覆盖。默认结果仅用于首轮候选优先级，后续应以实际路线、热处理、温度、载荷和边界重新筛选。
- 1111 图表发布路径已对齐 1115：`materials/modelfiles/image/{taskid}/alloy_composition_optimization/{taskid}/{filename}`，上传后验证对象存在。公开 PNG URL 已实测返回 HTTP 200 / `image/png`。
- 回归：`PYTHONPATH=. ALLOY_PRESENTATION_LLM=false /home/ubuntu/miniconda3/envs/ai4m-service-py310/bin/python -m unittest -q tests/test_service_contract.py`，16 passed；`git diff --check` 通过。仍未重启 tmux 或线上服务。

## 2026-09-01 续办：热端报告可读性

- 热端报告主标题调整为三级标题；默认热处理由原始阶段串改为“固溶处理 / 一级时效 / 二级时效”的温度—时间卡片。
- 页面明确说明实际模型为独立的 `HistGradientBoostingRegressor`（HGBR）回归器，不是 MLP；同时解释单晶路线、UTS（短时最大承载）、0.2% 屈服强度（抗永久变形）与蠕变断裂寿命的区别。
- 默认单来源情形不再向用户展示“来源锚点”列、数量或成分对照图；保留候选筛选路径与强度—寿命图。只有多个来源合金同时参与时，才显示成分追溯图。
- 优先候选结论增加完整性质卡：配方、工况、UTS、0.2% 屈服强度、蠕变寿命、延伸率、硬度标签状态与训练覆盖情况。
- 冒烟：`ni-hotend-presentation-check-20260901` 成功生成 12 个候选，仅输出两张面向用户的 PNG；1111 契约测试仍为 16 passed，`git diff --check` 通过。

## 2026-09-01 续办：单晶默认探索范围扩展

- 默认单晶 wt.% 边界由仅覆盖 Nasair 100 的窄范围，扩展为覆盖当前来源记录中 Nasair 100、CMSX-2、CMSX-4、PWA 1480、PWA 1484、René N4、René N5 的最小必要范围；新增 Re、V、Nb、Hf 等已存在于这些来源合金中的元素，并保留局部扰动余量。
- 实测默认工况可用参考合金从 1 个提升到 7 个，成功生成 12 个展示候选。该扩展仍是已记录单晶镍基合金附近的局部探索，不使用 0–100 wt.% 的无约束空间。
- 面向用户的成分追溯图改称“优先候选的配方调整对照”，仅在多个参考合金参与时显示；其作用是解释优先配方相对参考合金的元素调整来源，不用于比较性能。

## 2026-09-01 续办：热端图表布局

- `hot_end_screening_funnel.png` 已从普通柱状数量图改为与 1111 HEA 漏斗一致的分层收缩图，显示候选保留数量与比例。
- 热端 Markdown 使用图表占位符在发送前替换为公开图片链接：筛选漏斗紧接“筛选过程”，强度—寿命图紧接“优先候选”表，多个参考合金时的配方调整图紧接“完整性质卡”；不再统一堆叠在报告末尾。
- 冒烟任务 `ni-hotend-funnel-layout-check-20260901` 验证三张图均被替换到对应正文位置；1111 契约测试 16 passed，`git diff --check` 通过。

## 2026-09-01 续办：高温镍基机理特征离线消融

- 新增只读离线脚本：`/data/se42/hea_surrogate/tools/benchmark_ni_superalloy_mechanistic_features.py`。它严格复用现有 Tag-disjoint 划分和 HGBR 家族，比较 M0（当前服务特征的重新训练对照）与 M1（M0 + 原子序数/质量/半径/VEC 矩、尺寸失配、混合熵、γ′形成元素总量、Re/W/Mo 固溶强化元素总量、Cr/Co/微量元素组合及热处理派生量）。未接入 CALPHAD，因为尚未配置经验证的镍基热力学数据库。
- 首次固定留出结果写入 `hea_surrogate/reports/ni_superalloy/20260901_mechanistic_feature_ablation_v1/report.json`：UTS MAE 114.6 → 82.5 MPa；0.2% 屈服 MAE 79.8 → 50.2 MPa；蠕变 MAE 0.25866 → 0.25839 log10(h)。UTS/屈服仅分别有 5/4 个独立测试 Tag，故这一结果是有希望的离线信号，尚不足以替换线上模型；下一步需做重复 Tag 划分或 leave-one-tag-out 验证。
- 已完成逐 Tag 留出验证（每次将一个完整短时强度数据 Tag 排除，且该 Tag 不参与拟合或缺失值填补）。UTS：M0 → M1 的 pooled MAE 为 86.64 → 78.22 MPa，61 个 Tag 中 43 个改善、18 个变差；0.2% 屈服：79.15 → 74.61 MPa，57 个 Tag 中 32 个改善、25 个变差。M1 在未知 Tag 上仍有总体改善，但个别 Tag 的误差会显著增大；因此 M1 可作为受控的候选版本继续做 serving 环境复现实验和回退对比，暂不直接替换线上 M0。蠕变的固定留出改善不足，维持 M0。

## 2026-09-04 续办：热端 M1 候选与 runner 超时修复

- 已以线上 M0 的紧凑 31 维输入模式重建 M1 候选，避免此前离线完整热处理阶段特征与线上模式不一致。候选工件：`hea_surrogate/models/ni_superalloy_cast_ds_sc_strength_v0/m1_20260902_candidate/m1_models.joblib`；训练/验证脚本：`hea_surrogate/tools/prepare_ni_superalloy_strength_m1.py`；确定性描述符实现：`hea_surrogate/src/api/ni_superalloy_strength_features.py`。固定独立 Tag 测试（M0/M1 均使用相同的紧凑输入和候选 HGBR 配置）：UTS MAE 113.22 → 91.93 MPa，0.2% proof MAE 84.01 → 50.77 MPa。蠕变和延伸率维持原模型。
- `NI_SUPERALLOY_STRENGTH_VARIANT` 现支持 `m0`（默认）与 `m1_candidate`。仅在服务进程启动前设置为 `m1_candidate` 时，隔离 MatterGen runner 才加载 M1 UTS/proof 工件；默认不变且可通过去除/设为 `m0` 回退。服务 Web Python 不直接加载 sklearn 工件，仍由 `HEASurrogateRunner` 的 micromamba 子进程加载。
- 2026-09-04 曾出现 `TimeoutExpired(..., 90)`：请求本身为单晶、1050 °C、200 MPa、120 个候选，未生成 `runner_response.json`。根因是隔离进程未限制 BLAS/OpenMP 线程，小批量 HGBR 预测发生过度并行。runner 现对其子进程固定 `OMP_NUM_THREADS=1`、`OPENBLAS_NUM_THREADS=1`、`MKL_NUM_THREADS=1`，并继续保留任务级 `XDG_CACHE_HOME`，防止 micromamba 全局锁竞争。使用原请求的 runner 冒烟为 2.81 秒，成功返回 120 个候选；在 UTS≥900 MPa、proof≥500 MPa、寿命≥250 h 三个门槛下短名单为 0，这是工况/门槛筛选结果而非执行错误。
- 验证：M0/M1 同输入加载冒烟、M1 经 Web→micromamba 隔离 runner 链路加载冒烟均通过；`PYTHONPATH=hea_surrogate ... python -m unittest -q hea_surrogate.tests.test_ni_superalloy_hotend_runtime` 为 2 passed；`git -C alpha_project/material_service_hub diff --check` 通过。未重启任何服务或 tmux。

## 2026-09-04 续办：严格门槛的最接近候选回退

- `ni_superalloy_hot_end.propose()` 现将“严格通过的短名单”和“最接近门槛的候选”分开返回：`initial_candidates` 只包含同时满足 UTS、0.2% proof 与蠕变寿命门槛的候选；严格通过数为零时，`nearest_candidates` 返回相对三项门槛综合缺口最小的 3 个候选，并对每项记录目标、预测值、绝对缺口和相对缺口。
- 1111 结果页在严格通过数为零时，仍沿用候选表、性质卡和图表的正常流程；以“暂未配比出同时严格满足全部需求的材料；以下为最接近目标的后续方向”承接，并在表中列出主要性能差距。强度—寿命图星标改为“下一步优先评估”。
- 使用此前超时请求复测：单晶、1050 °C、200 MPa、UTS≥900 MPa、proof≥500 MPa、寿命≥250 h，严格通过数为 0；最接近的三个候选寿命为 285.5–290.0 h，但 UTS 400.0–401.0 MPa、proof 297.6–306.9 MPa，主要缺口是短时强度与屈服强度，不是寿命。
- 验证：`hea_surrogate.tests.test_ni_superalloy_hotend_runtime` 现 3 passed（包含严格无通过时的回退候选断言）；相关四个 Python 文件 `py_compile`、`git diff --check` 均通过。仍未重启服务或 tmux，需用户在既有 1111 tmux 重启后生效。

# Config Layout

- `config.yaml`: active runtime config for current inorganic existing material main flow.
- `legacy/config2.yaml`: legacy alpha-framework style config, kept for compatibility reference.
- `legacy/puppeteer-config.json`: legacy browser config, not used by current main flow by default.

Updated: 2026-05-15
